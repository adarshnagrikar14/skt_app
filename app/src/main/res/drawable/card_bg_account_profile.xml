<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <solid android:color="@color/secondary" />

    <!-- Rounded corners -->
    <corners android:radius="@dimen/dimen_12sp" />

    <!-- Border -->
    <stroke
        android:width="1dp"
        android:color="#000000" />
</shape>
